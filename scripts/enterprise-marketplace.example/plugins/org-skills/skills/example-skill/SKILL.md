---
description: >-
  Placeholder enterprise skill - replace this file's body with real
  instructions and this description with one that tells Claude when to use
  the skill (the description is what triggers model-side invocation).
---

Replace this body with the skill's instructions to Claude: the steps to
follow, the conventions to apply, and any commands or file locations it
needs. This skill is invoked as `/org-skills:example-skill` (plugin name,
colon, this folder's name); rename the folder to rename the skill.

Keep the body focused and reasonably short — it is loaded into the model's
context when the skill triggers. Reference files elsewhere in the plugin by
relative path rather than inlining large content here.
