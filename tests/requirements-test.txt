# Python test dependencies (pip install -r tests/requirements-test.txt)
pytest>=8
boto3>=1.34
moto[secretsmanager]>=5
pg8000>=1.31
cfn-lint>=1
pyyaml>=6
# Portal tests mint RSA-signed test ID tokens + JWKS (the portal app itself
# verifies RS256 in pure Python with no crypto dependency).
cryptography>=41
